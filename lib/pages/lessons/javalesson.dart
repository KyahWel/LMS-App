import 'package:flutter/material.dart';

class JavaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is the Java page.'),
      ),
    );
  }
}
